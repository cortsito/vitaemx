"""Reusable research code for VitaeMX.

The notebooks in ``research/`` are thin: they load data, call the functions
in this package, look at the results and write them to ``data/processed/``.
Keeping the math here (instead of inline in notebook cells) is what makes it
unit-testable (see ``research/tests/``) and reviewable as ordinary Python.
"""
